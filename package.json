{
  "name": "two-cents",
  "version": "0.0.0",
  "description": "Personal finance app — pull, categorize, and budget your own bank transactions.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "daisyui": "^5.5.14",
    "dotenv": "^17.3.1",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3"
  }
}
